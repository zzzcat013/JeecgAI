# 后端辅助类文档

用于存放 `jeecg-boot` 相关的辅助类/工具文档。

建议按模块划分：
- `base-core/` 核心与通用能力
- `module/` 业务模块（示例：`jeecg-module-system`、`jeecg-module-demo`）
- `server-cloud/` 微服务与中间件（网关、Nacos、Sentinel、监控、XXL-Job 等）

示例命名：
- `system/<类名>.md`
- `base-core/<类名>.md`
- `cloud-gateway/<类名>.md`

